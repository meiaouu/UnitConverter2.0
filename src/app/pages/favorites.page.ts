import { CommonModule } from '@angular/common'; import { Component } from '@angular/core'; import { Router } from '@angular/router'; import { IonContent,IonIcon } from '@ionic/angular/standalone'; import { addIcons } from 'ionicons'; import { starOutline,star,trashOutline,chevronForwardOutline } from 'ionicons/icons'; import { AppStateService } from '../core/services/app-state.service'; import { ConversionService } from '../core/services/conversion.service'; import { BottomNavComponent } from '../shared/components/bottom-nav/bottom-nav.component';
@Component({standalone:true,imports:[CommonModule,IonContent,IonIcon,BottomNavComponent],template:`<ion-content><main class="page-shell"><header class="topbar"><h1>Favorites</h1></header><p class="eyebrow">YOUR SHORTCUTS</p><h1 class="hero-title">Favorite pairs</h1><p class="intro">Keep the conversions you use most within reach.</p><div class="empty-state" *ngIf="!state.favorites.length"><ion-icon name="star-outline"></ion-icon><h2>No favorites yet</h2><p>Tap the star on a converter to save a pair.</p></div><div class="list-cards" *ngIf="state.favorites.length"><article *ngFor="let f of state.favorites"><button class="list-main" (click)="open(f)"><ion-icon name="star" class="favorite-icon"></ion-icon><span><b>{{pair(f)}}</b><small>{{category(f.categoryId)}}</small></span><ion-icon name="chevron-forward-outline"></ion-icon></button><button class="delete-btn" (click)="remove(f)" aria-label="Remove favorite"><ion-icon name="trash-outline"></ion-icon></button></article></div></main></ion-content><app-bottom-nav active="favorites"></app-bottom-nav>`})
export class FavoritesPage{constructor(public state:AppStateService,private conv:ConversionService,private router:Router){addIcons({starOutline,star,trashOutline,chevronForwardOutline})}category(id:string){return this.conv.getCategory(id)?.name||id}pair(f:any){const c=this.conv.getCategory(f.categoryId);return `${c?.units.find(u=>u.id===f.fromUnitId)?.name||f.fromUnitId} → ${c?.units.find(u=>u.id===f.toUnitId)?.name||f.toUnitId}`}open(f:any){this.router.navigate(['/converter',f.categoryId],{queryParams:{from:f.fromUnitId,to:f.toUnitId}})}remove(f:any){this.state.toggleFavorite(f.categoryId,f.fromUnitId,f.toUnitId)}}
