import { Component } from '@angular/core'; import { Router } from '@angular/router'; import { IonContent, IonIcon } from '@ionic/angular/standalone'; import { addIcons } from 'ionicons'; import { swapVerticalOutline } from 'ionicons/icons'; import { AppStateService } from '../core/services/app-state.service';
@Component({standalone:true,imports:[IonContent,IonIcon],template:`<ion-content class="welcome-content"><main class="welcome"><div class="welcome-mark"><ion-icon name="swap-vertical-outline"></ion-icon><span>1 ↔ m</span></div><p class="eyebrow">UNIT CONVERTER</p><h1>Convert anything,<br><em>anywhere.</em></h1><p class="welcome-sub">Fast, accurate, and simple unit conversions.</p><div class="example"><b>100 m</b><span>→</span><b>328.08 ft</b></div><button class="primary-btn" (click)="start()">GET STARTED</button><small>No account required.</small></main></ion-content>`})
export class WelcomePage{constructor(private state:AppStateService,private router:Router){addIcons({swapVerticalOutline})}start(){this.state.completeOnboarding();this.router.navigateByUrl('/home',{replaceUrl:true})}}
