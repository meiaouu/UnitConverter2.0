import { CommonModule } from '@angular/common'; import { Component } from '@angular/core'; import { Router } from '@angular/router'; import { IonContent,IonIcon } from '@ionic/angular/standalone'; import { AlertController } from '@ionic/angular'; import { addIcons } from 'ionicons'; import { timeOutline,trashOutline,chevronForwardOutline } from 'ionicons/icons'; import { AppStateService } from '../core/services/app-state.service'; import { ConversionService } from '../core/services/conversion.service'; import { BottomNavComponent } from '../shared/components/bottom-nav/bottom-nav.component';
@Component({standalone:true,imports:[CommonModule,IonContent,IonIcon,BottomNavComponent],template:`<ion-content><main class="page-shell"><header class="topbar"><h1>Recent</h1></header><p class="eyebrow">YOUR DEVICE HISTORY</p><div class="title-row"><div><h1 class="hero-title">Recent conversions</h1><p class="intro">Your history stays private and offline on this device.</p></div><button class="text-btn" *ngIf="state.recent.length" (click)="clearAll()">Clear all</button></div><div class="empty-state" *ngIf="!state.recent.length"><ion-icon name="time-outline"></ion-icon><h2>No recent conversions yet</h2><p>Your saved results will appear here.</p></div><div class="list-cards" *ngIf="state.recent.length"><article *ngFor="let r of state.recent"><button class="list-main" (click)="open(r)"><span><b>{{r.fromText}} → {{r.toText}}</b><small>{{name(r.categoryId)}} · {{relative(r.createdAt)}}</small></span><ion-icon name="chevron-forward-outline"></ion-icon></button><button class="delete-btn" (click)="state.deleteRecent(r.id)" aria-label="Delete conversion"><ion-icon name="trash-outline"></ion-icon></button></article></div></main></ion-content><app-bottom-nav active="recent"></app-bottom-nav>`})
export class RecentPage{constructor(public state:AppStateService,private conv:ConversionService,private router:Router,private alert:AlertController){addIcons({timeOutline,trashOutline,chevronForwardOutline})}name(id:string){return this.conv.getCategory(id)?.name||id}open(r:any){this.router.navigate(['/converter',r.categoryId],{queryParams:{from:r.fromUnitId,to:r.toUnitId,value:r.input}})}relative(ts:number){const s=Math.floor((Date.now()-ts)/1000);return s<60?'Just now':s<3600?`${Math.floor(s/60)}m ago`:s<86400?`${Math.floor(s/3600)}h ago`:new Date(ts).toLocaleDateString()}async clearAll(){const a=await this.alert.create({header:'Clear history?',message:'This removes all recent conversions from this device.',buttons:[{text:'Cancel',role:'cancel'},{text:'Clear',role:'destructive',handler:()=>this.state.clearRecent()}]});await a.present()}}
